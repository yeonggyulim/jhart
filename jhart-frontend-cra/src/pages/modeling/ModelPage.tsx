import React from 'react';
import { Header, Footer, Navigation } from '../../components';

const ModelPage = () => {
	return (
		<>
			<Header />
			<Navigation />
			<div>Modeling</div>
			<Footer />
		</>
	);
};

export default ModelPage;
